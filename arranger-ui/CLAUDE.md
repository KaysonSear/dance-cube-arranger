@AGENTS.md

Project spec (功能定义/数据结构/交互状态机/验收清单): `spec.md` — read it before changing
editing semantics, timing math, or export behavior. Repo-wide invariants live in the root
`CLAUDE.md` (WDA/ read-only; outputs under `artifacts/arranger/`; `configs/column_geometry.json`
fetched at runtime, never hardcoded; offset written back verbatim).
